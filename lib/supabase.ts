/**
 * Supabase-ready placeholder.
 * Do not instantiate a client until a server-only package and env vars are approved.
 */
export const supabaseEnvKeys = [
  'NEXT_PUBLIC_SUPABASE_URL',
  'SUPABASE_SERVICE_ROLE_KEY',
] as const;
